### CAPÍTULO 6 – Otimização, Modelo Físico e Administração Básica

O projeto de um banco de dados (BD) é concluído com a escolha do Sistema Gerenciador de Banco de Dados (SGBD) e a criação do **Modelo Físico**. Essa fase envolve a **otimização** do sistema, buscando garantir que as consultas e operações sejam executadas de forma eficiente, uma capacidade que todo bom SGBD deve possuir.

🖥️ **Ferramenta:** pgAdmin (PostgreSQL)

## 6.1 Conceito de Modelo Físico e Mapeamento para o SGBD.

O **Projeto Físico** é a etapa final do projeto de banco de dados, sendo o processo de refinamento do modelo lógico.

1.  **Nível de Abstração:** O Modelo Físico é o **nível mais baixo de abstração**. Ele descreve como os dados serão armazenados fisicamente no computador.
2.  **Estrutura e Detalhamento:** Descreve a implementação do BD, englobando aspectos de *software* e *hardware*. Ele especifica o **tamanho de campos** e os **tipos dos campos**, além da terminologia dos campos, que são projetadas de acordo com os requisitos de processamento.
3.  **Dependência do SGBD:** Ao contrário dos modelos conceitual e lógico (que são mais independentes do *software*), o projeto físico é dependente do SGBD escolhido (como o PostgreSQL ou MySQL). O modelo físico especifica as estruturas de armazenamento, os **índices** e os caminhos de acesso ao BD.
4.  **Implementação:** A implementação do modelo físico é realizada utilizando comandos da **Linguagem de Definição de Dados (DDL)**, como o `CREATE TABLE`.

## 6.2 Índices: Criação, Tipos e Desempenho de Consultas.

Os índices são cruciais para garantir o desempenho das consultas e a eficiência do sistema.

**Função e Natureza:**

O SGBD deve prover estruturas de dados especializadas e técnicas de busca que aceleram a pesquisa no disco por registros de interesse. Os **índices** são arquivos auxiliares (ou estruturas de dados especializadas) frequentemente utilizados para esse propósito. O projeto físico do BD deve especificar os **índices**.

**Tipos e Estrutura:**

*   Os índices são tipicamente baseados em uma **estrutura de dados em árvore ou *hash***, modificadas para serem adequadas à busca em disco.
*   As chaves primárias (PKs) são importantes porque permitem encontrar tuplas específicas dentro de um conjunto de tuplas de forma **aleatória**, evitando a necessidade de buscar os registros de forma sequencial.

**Criação (DDL):**

A Linguagem de Definição de Dados (DDL) é o subconjunto da SQL que inclui comandos para a criação de índices. O comando `CREATE INDEX` é usado para criar índices em uma tabela.

## 6.3 Ajustes de Normalização e Desnormalização para Performance.

O projeto do banco de dados busca, idealmente, armazenar cada item lógico de dado em apenas um lugar, o que é alcançado através da **normalização de dados**.

### Normalização (Controle de Redundância)

*   A normalização é um processo de análise realizado sobre esquemas relacionais para minimizar a **redundância de dados** e, consequentemente, reduzir anomalias (de inserção, atualização e exclusão).
*   A redundância de dados ocorre quando a mesma informação é armazenada múltiplas vezes no sistema.
*   A redundância gera problemas como o desperdício de espaço de armazenamento (crítico em grandes BDs) e inconsistência (se a informação não for atualizada em todas as cópias).

### Desnormalização (Ajuste para Performance)

A **desnormalização** é um assunto controverso na literatura e não é uma técnica unânime. Ela envolve a introdução intencional de redundância, combinando atributos não relacionados ou mantendo-os em tipos de entidade mais simples.

A desnormalização pode ser necessária em casos específicos:

1.  **Histórico de Dados:** Quando é necessário manter um histórico de dados armazenados, como documentos fiscais que, após consolidados, não podem ser alterados.
2.  **Consultas Complexas:** Para evitar a necessidade de consultas complexas que são requeridas constantemente, como aquelas que exigem junções entre duas ou mais tabelas.
3.  **Ganho de Desempenho (Controverso):** Desnormalizar para obter ganho de desempenho é o argumento mais controverso, pois é difícil comprovar que o aumento de performance é significativo.

## 6.4 Plano de Execução (Execution Plan) e Tuning Básico.

Embora os termos específicos *Plano de Execução* e *Tuning* não estejam detalhados nas fontes, o conceito de otimização e eficiência é fundamental para o SGBD.

O SGBD deve prover a capacidade de executar consultas e atualizações de forma **eficiente**.

**Tuning Básico e Otimização:**

1.  **Utilização de Índices:** O uso de arquivos auxiliares chamados **índices** é a técnica primária fornecida pelo SGBD para acelerar a busca no disco por registros de interesse.
2.  **Projeto Adequado:** Garantir que o BD esteja em um estado consistente (normalizado) é a base para a eficiência, pois um projeto mal elaborado pode apresentar anomalias (inclusão, alteração, exclusão) que dificultam a manutenção e a consulta.
3.  **Responsabilidade do DBA:** O Administrador do Banco de Dados (DBA) é o profissional responsável por monitorar e gerenciar todas as bases de dados criadas no SGBD. Uma das funções do DBA é garantir que os dados estejam com **desempenho satisfatório**.

## 6.5 ESTUDO DE CASO: Otimizando Consultas Críticas da Biblioteca. (Prática relacionada ao conteúdo do capítulo no contexto do sistema da biblioteca.)

Em um BD de Biblioteca (que contém dados sobre alunos, disciplinas/livros e solicitações/empréstimos), consultas críticas frequentemente envolvem buscas por nome de aluno, título de livro ou datas.

**Prática de Otimização (Uso de Índices):**

Se a tabela `LIVRO` contiver milhões de registros, uma consulta como `SELECT * FROM LIVRO WHERE Titulo = 'Titulo X'` será lenta se o atributo `Titulo` não for indexado.

**Ação:** Criação de um índice no atributo `Titulo` (e possivelmente no `Nome` do aluno na tabela `ALUNO`) para acelerar as operações de busca que utilizam esses campos na cláusula `WHERE`.

**Comando DDL (Conceitual para PostgreSQL/pgAdmin):**

```sql
-- DDL: Criação de Índice no campo Titulo da tabela LIVRO
CREATE INDEX idx_titulo_livro ON LIVRO (Titulo);

-- DDL: Criação de Índice no campo Nome da tabela ALUNO
CREATE INDEX idx_nome_aluno ON ALUNO (Nome);
```

## 6.6 EXERCÍCIOS COMPLEMENTARES: Criação de índices em tabelas de grande volume (Ex: Log de Acessos).

Em um domínio de grande volume de dados, como um *Log de Acessos* (`TB_LOG_ACESSO`), onde cada registro contém `ID_Sessao`, `Data_Hora_Acesso` e `IP_Usuario`.

**Objetivo:** Otimizar consultas que buscam rapidamente todos os acessos realizados em uma data específica.

1.  **Criação de Tabela (DDL):** (Assumindo a tabela `TB_LOG_ACESSO` já existe).
2.  **Identificação do Ponto Crítico:** O ponto crítico é o filtro por `Data_Hora_Acesso`.
3.  **Criação de Índice (DDL):** Crie um índice no campo `Data_Hora_Acesso` para permitir que o SGBD localize rapidamente os registros, acelerando o processo de busca no disco.

**Comando DDL (Conceitual para PostgreSQL/pgAdmin):**

```sql
-- Criação de Índice para acelerar consultas baseadas em tempo/data
CREATE INDEX idx_data_hora_acesso ON TB_LOG_ACESSO (Data_Hora_Acesso);
```

## 6.7 EXERCÍCIOS INTEGRADOS: Otimizando Consultas em Dados Científicos (Química/Biologia).

Em um domínio de dados científicos (ex: Biologia/Química), um banco de dados pode armazenar `TB_AMOSTRA` e `TB_RESULTADO_ANALISE`. O desempenho é crítico ao correlacionar resultados de análise com as amostras.

**Cenário:** A tabela `TB_RESULTADO_ANALISE` possui chaves primárias compostas por `(ID_Amostra, ID_Teste)` e um atributo `Valor_Medido`.

**Desafio de Otimização (Desnormalização vs. Normalização):**

Suponha que 99% das consultas exigem a `Descricao_Amostra` da tabela `TB_AMOSTRA` junto com o `Valor_Medido` da `TB_RESULTADO_ANALISE`.

**Pergunta:** Qual seria um ajuste de desempenho controverso, porém prático, que poderia ser feito (desconsiderando a 3FN para esta análise de performance)?

**Resposta:** A desnormalização. Para evitar a constante necessidade de `JOIN` (junção) entre `TB_AMOSTRA` e `TB_RESULTADO_ANALISE`, o atributo `Descricao_Amostra` poderia ser redundantemente copiado para a tabela `TB_RESULTADO_ANALISE`. Isso aceleraria as consultas frequentes que envolvem apenas essas duas colunas, embora aumente a redundância e a complexidade de atualização dos dados.

*Nota: Este é um exemplo de trade-off de desempenho em que a desnormalização é aplicada apesar das desvantagens de integridade, conforme discutido na seção 6.3.*
